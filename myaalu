<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>For Priyanka 💖</title>

<style>
  body {
    margin: 0;
    height: 100vh;
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    font-family: 'Comic Sans MS', cursive;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    overflow: hidden;
  }

  .container {
    background: white;
    padding: 30px;
    border-radius: 25px;
    box-shadow: 0 20px 40px rgba(0,0,0,0.2);
    max-width: 90%;
    position: relative;
  }

  h1 {
    font-size: 28px;
    color: #ff4d6d;
  }

  p {
    font-size: 18px;
    margin-bottom: 20px;
  }

  button {
    font-size: 20px;
    padding: 15px 25px;
    border: none;
    border-radius: 30px;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  #yesBtn {
    background: #ff4d6d;
    color: white;
  }

  #noBtn {
    background: #ccc;
    color: black;
    position: absolute;
  }

  .heart {
    font-size: 80px;
    animation: pulse 1s infinite;
  }

  @keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.4); }
    100% { transform: scale(1); }
  }
</style>
</head>

<body>

<div class="container" id="box">
  <h1>Priyanka 💕</h1>
  <p id="msg">Will you be my Valentine? 💘</p>

  <button id="yesBtn">YES 💖</button>
  <button id="noBtn">NO 🙃</button>
</div>

<!-- Sound when clicking NO -->
<audio id="noSound">
  <source src="https://cdn.pixabay.com/download/audio/2022/03/10/audio_6d1b6b9b96.mp3" type="audio/mpeg">
</audio>

<!-- Romantic music for YES -->
<audio id="loveMusic" loop>
  <source src="https://cdn.pixabay.com/download/audio/2022/03/15/audio_5d6c9b9d69.mp3" type="audio/mpeg">
</audio>

<script>
  const noBtn = document.getElementById("noBtn");
  const yesBtn = document.getElementById("yesBtn");
  const msg = document.getElementById("msg");
  const box = document.getElementById("box");
  const noSound = document.getElementById("noSound");
  const loveMusic = document.getElementById("loveMusic");

  let yesScale = 1;
  let noCount = 0;

  const noTexts = [
    "Oops 🥺",
    "That hurt 😭",
    "Try again 🙃",
    "Please? 🥹",
    "Okay… but NO is shy 😏"
  ];

  function moveNo() {
    noSound.currentTime = 0;
    noSound.play();

    msg.innerText = noTexts[noCount % noTexts.length];
    noCount++;

    const x = Math.random() * (window.innerWidth - 120);
    const y = Math.random() * (window.innerHeight - 80);

    noBtn.style.left = x + "px";
    noBtn.style.top = y + "px";

    yesScale += 0.25;
    yesBtn.style.transform = `scale(${yesScale})`;
  }

  noBtn.addEventListener("touchstart", moveNo);
  noBtn.addEventListener("mouseover", moveNo);

  yesBtn.addEventListener("click", () => {
    loveMusic.play();

    box.innerHTML = `
      <div class="heart">❤️</div>
      <h1>YAYYY PRIYANKA 🥰</h1>
      <p>You just made my heart very happy 💖</p>
      <p>Happy Valentine’s Day ❤️</p>
    `;
  });
</script>

</body>
</html>
